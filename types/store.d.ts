export interface AppState {
    slug:String,
    mode:String,
    shadow:String
}

export interface Sluy {
    slug:String,
}

export interface Mode {
    mode:String
}
export interface Shadow {
    shadow:Boolean
}
