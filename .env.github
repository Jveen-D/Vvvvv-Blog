# github pages的配置，当使用这个配置文件的时候，tailwindcss不会自动将未使用的CSS样式去除，因为tailwindcss默认只在生产模式中才会将多余的css剔除
NODE_ENV=github

# OUTPUT_DIR
VITE_OUTPUT_DIR = docs

# public path
VITE_PUBLIC_PATH = ./

# Delete console
VITE_DROP_CONSOLE = true

# Cross-domain proxy, you can configure multiple
# Please note that no line breaks
VITE_PROXY = [['/api','http://124.71.57.28:8090/api']]
# VITE_PROXY=[["/api","https://vvbin.cn/test"]]
